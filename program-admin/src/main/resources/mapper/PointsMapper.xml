<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.program.mapper.PointsMapper">
    
<!--    <select id="selectList" resultType="com.program.model.entity.Points">-->
<!--        select * from points-->
<!--        <where>  -->
<!--            <if test="username != null  and username != ''"> and username like concat('%', #{username}, '%')</if>-->
<!--            <if test="userId != null "> and user_id = #{userId}</if>-->
<!--            <if test="beginTime != null and beginTime != '' and endTime != null and endTime != ''"> and points_flow between #{beginTime} and #{endTime}</if>-->
<!--            <if test="notes != null  and notes != ''"> and notes like concat('%', #{notes}, '%')</if>-->
<!--            <if test="obtainMethod != null "> and obtain_method = #{obtainMethod}</if>-->
<!--            <if test="isDeleted != null "> and is_deleted = #{isDeleted}</if>-->
<!--        </where>-->
<!--    </select>-->

</mapper>